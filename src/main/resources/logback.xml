<?xml version="1.0" encoding="UTF-8"?>

<configuration>
  <!-- 控制台输出 -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{traceId}/%X{spanId}] [%thread] %highlight(%-5level) %logger{50} - %replace(%msg){'\d{14,16}', '***'} %replace(%msg){'[12][0-9]{3}[01][0-9][0123][0-9]','********'}%n</pattern>
    </encoder>
  </appender>
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <!-- 日志文件 -->
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>logs/wallet_server.%d{yyyy-MM-dd}.log</fileNamePattern>
      <maxHistory>60</maxHistory>
    </rollingPolicy>

    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{traceId}/%X{spanId}] [%thread] %highlight(%-5level) %logger{50} - %replace(%msg){'\d{14,16}', '***'} %replace(%msg){'[12][0-9]{3}[01][0-9][0123][0-9]','********'}%n</pattern>
    </encoder>
  </appender>

  <logger name="com.rfchina" level="INFO" additivity="false">
    <appender-ref ref="FILE"/>
    <appender-ref ref="CONSOLE"/>
  </logger>

  <root level="ERROR">
    <appender-ref ref="FILE"/>
    <appender-ref ref="CONSOLE"/>
  </root>

</configuration>